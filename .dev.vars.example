# Admin dashboard login key. Required locally and in Cloudflare Secrets.
# Copy to .dev.vars and fill in a value generated by: openssl rand -hex 32
API_KEY=""

# Configure bot tokens and application API keys in the dashboard.
